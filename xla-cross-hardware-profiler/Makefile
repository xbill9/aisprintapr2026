install:
	pip install -r requirements.txt

run:
	python server.py

test:
	@echo "No test script found in xla-cross-hardware-profiler"

clean:
	rm -rf __pycache__
	find . -type d -name "__pycache__" -exec rm -rf {} +
